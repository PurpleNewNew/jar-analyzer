<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ GPLv3 License
  ~
  ~ Copyright (c) 2022-2026 4ra1n (Jar Analyzer Team)
  ~
  ~ This project is distributed under the GPLv3 license.
  ~
  ~ https://github.com/jar-analyzer/jar-analyzer/blob/master/LICENSE
  -->

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="me.n1ar4.jar.analyzer.core.mapper.ResourceMapper">
    <resultMap id="resourceMap" type="me.n1ar4.jar.analyzer.entity.ResourceEntity">
        <result column="rid" property="rid"/>
        <result column="jar_id" property="jarId"/>
        <result column="jar_name" property="jarName"/>
        <result column="resource_path" property="resourcePath"/>
        <result column="path_str" property="pathStr"/>
        <result column="file_size" property="fileSize"/>
        <result column="is_text" property="isText"/>
    </resultMap>

    <insert id="insertResources" parameterType="ResourceEntity">
        INSERT INTO resource_table
        (resource_path, path_str, jar_name, jar_id, file_size, is_text)
        VALUES
        <foreach collection="list" separator="," item="item">
            (#{item.resourcePath}, #{item.pathStr}, #{item.jarName}, #{item.jarId},
            #{item.fileSize}, #{item.isText})
        </foreach>
    </insert>

    <select id="selectResources" resultMap="resourceMap">
        SELECT rid, jar_id, jar_name, resource_path, path_str, file_size, is_text
        FROM resource_table
        <where>
            <if test="path != null and path != ''">
                resource_path LIKE CONCAT('%', #{path}, '%')
            </if>
            <if test="jarId != null">
                AND jar_id = #{jarId}
            </if>
        </where>
        ORDER BY resource_path COLLATE NOCASE
        LIMIT #{limit}
        OFFSET #{offset}
    </select>

    <select id="selectCount">
        SELECT COUNT(1)
        FROM resource_table
        <where>
            <if test="path != null and path != ''">
                resource_path LIKE CONCAT('%', #{path}, '%')
            </if>
            <if test="jarId != null">
                AND jar_id = #{jarId}
            </if>
        </where>
    </select>

    <select id="selectResourceById" resultMap="resourceMap">
        SELECT rid, jar_id, jar_name, resource_path, path_str, file_size, is_text
        FROM resource_table
        WHERE rid = #{rid}
        LIMIT 1
    </select>

    <select id="selectResourceByPath" resultMap="resourceMap">
        SELECT rid, jar_id, jar_name, resource_path, path_str, file_size, is_text
        FROM resource_table
        <where>
            resource_path = #{path}
            <if test="jarId != null">
                AND jar_id = #{jarId}
            </if>
        </where>
        LIMIT 1
    </select>

    <select id="selectTextResources" resultMap="resourceMap">
        SELECT rid, jar_id, jar_name, resource_path, path_str, file_size, is_text
        FROM resource_table
        <where>
            is_text = 1
            <if test="jarId != null">
                AND jar_id = #{jarId}
            </if>
        </where>
        ORDER BY resource_path COLLATE NOCASE
    </select>
</mapper>
