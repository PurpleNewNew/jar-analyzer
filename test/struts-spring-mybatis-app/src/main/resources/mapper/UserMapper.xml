<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="com.example.ssm.mapper.UserMapper">
    <select id="findUserByKeyword" parameterType="string" resultType="string">
        SELECT username FROM users WHERE username LIKE '%${keyword}%'
    </select>
</mapper>
